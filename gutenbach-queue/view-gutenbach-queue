#!/bin/bash

daemon --running --name gutenbach-queue -F /var/run/sipbmp3/gutenbach-queue.pid
if [ "$?" == 0 ]; then
    watch -t -n 1 cat /var/run/sipbmp3/current_queue
else
    echo "The gutenbach queue daemon is not running.  Please start it at /etc/init.d/gutenbach-queue."
fi
